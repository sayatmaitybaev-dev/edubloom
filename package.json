{
  "name": "edubloom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm install --omit=dev && npx prisma generate && npx prisma migrate deploy && node dist/index.js",
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci && npm run build",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
